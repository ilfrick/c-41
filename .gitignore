build*/
obj*/
/target/
Cargo.lock
*~
*.pot
*.mo
doc/usermanual/darktable-usermanual*.pdf
doc/api/
tools/iop_deps.pdf
*.kdev4
.DS_Store
cscope.out
.*.swp
.vscode/
.clangd/
.cache
tags
cscope.*
compile_commands.json
.cproject
.project
.settings/
diff*png
output*png
*.code-workspace
Brewfile.lock.json
CMakeLists.txt.user
workspace/
cmake-build-debug/
.idea/
.env

# local raw samples for manual verification (not committed)
testdata/

# Container runtime state — the bind-mounted /config for the c-41 container.
# Holds the user's live catalogue (library.db, data.db), thumbnail cache and
# KasmVNC session state/certs. Committed by accident in m4-109 via `git add -A`;
# it is machine-local user data, not source, and library.db churns on every run.
/config/

# Local ci-local.sh gate logs (scratch, never ship)
gate-*.log
